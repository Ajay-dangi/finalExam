apiVersion: batch/v1
kind: CronJob
metadata:
  name: job-email-pending-pods-info
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: pending-pods-observer
              image: ajay231998/question4:latest
              imagePullPolicy: IfNotPresent
          restartPolicy: OnFailure
